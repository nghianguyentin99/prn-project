<UserControl x:Class="PCShop.View.Shared.Header"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Height="80" Background="#2C3E50">

    <Grid Margin="15,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <!-- Logo -->
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
            <Ellipse Width="40" Height="40" Fill="#3498DB" Margin="0,0,10,0"/>
            <TextBlock Text="PCSHOP" Foreground="White" FontSize="22" FontWeight="Bold" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Tiêu đề -->
        <TextBlock Grid.Column="1" Text="HỆ THỐNG QUẢN LÝ KHO" 
                   Foreground="White" FontSize="20" 
                   FontWeight="SemiBold" VerticalAlignment="Center"
                   HorizontalAlignment="Center"/>

        <!-- User info -->
        <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
            <TextBlock x:Name="txtUser" Text="Xin chào, Admin" 
                       Foreground="White" FontSize="14" Margin="0,0,10,0"/>
            <Button Content="Đăng xuất" Background="#E74C3C" Foreground="White"
                    Padding="10,5" BorderThickness="0" Click="BtnLogout_Click"/>
        </StackPanel>
    </Grid>
</UserControl>


