<UserControl x:Class="PCShop.View.Admin.ApprovalView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PCShop.View.Admin"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900"
             Background="White" Loaded="UserControl_Loaded">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="4*" />
            <ColumnDefinition Width="5*" />
        </Grid.ColumnDefinitions>

        <GroupBox Grid.Column="0" Header="Danh sách phiếu chờ duyệt" Padding="5" Margin="0,0,10,0">
            <DataGrid x:Name="dgPendingRequests"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      SelectionMode="Single"
                      SelectionChanged="dgPendingRequests_SelectionChanged">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding EntryId}" Width="Auto" />
                    <DataGridTextColumn Header="Người tạo" Binding="{Binding User.FullName}" Width="*" />
                    <DataGridTextColumn Header="NCC" Binding="{Binding Supplier.Name}" Width="Auto" />
                    <DataGridTextColumn Header="Ngày tạo" Binding="{Binding EntryDate, StringFormat='dd/MM/yy HH:mm'}" Width="Auto" />
                    <DataGridTextColumn Header="Tổng tiền" Binding="{Binding TotalAmount, StringFormat='N0'}" Width="Auto" />
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <GroupBox Grid.Column="1" Header="Chi tiết yêu cầu" x:Name="gbRequestDetails" IsEnabled="False">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="5">
                    <WrapPanel>
                        <TextBlock Text="Phiếu số: " FontWeight="Bold" />
                        <TextBlock x:Name="txtDetailEntryId" />
                    </WrapPanel>
                    <WrapPanel>
                        <TextBlock Text="Ghi chú (từ Staff): " FontWeight="Bold" />
                        <TextBlock x:Name="txtDetailNote" TextWrapping="Wrap" />
                    </WrapPanel>
                </StackPanel>

                <DataGrid Grid.Row="1" x:Name="dgRequestDetails"
                          AutoGenerateColumns="False" IsReadOnly="True" Margin="5"
                          HeadersVisibility="Column">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Tên sản phẩm" Binding="{Binding Product.Name}" Width="*" />
                        <DataGridTextColumn Header="Số lượng" Binding="{Binding Quantity}" Width="Auto" />
                        <DataGridTextColumn Header="Đơn giá" Binding="{Binding UnitPrice, StringFormat='N0'}" Width="Auto" />
                        <DataGridTextColumn Header="Thành tiền" Binding="{Binding TotalPrice, StringFormat='N0'}" Width="Auto" />
                    </DataGrid.Columns>
                </DataGrid>

                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="5">
                    <Button x:Name="btnReject" Content="Từ chối" Padding="20,5" Margin="0,0,10,0"
                            Background="IndianRed" Foreground="White" Click="btnReject_Click" />
                    <Button x:Name="btnApprove" Content="Phê duyệt" Padding="20,5"
                            Background="SeaGreen" Foreground="White" FontWeight="Bold" Click="btnApprove_Click" />
                </StackPanel>
            </Grid>
        </GroupBox>

    </Grid>
</UserControl>