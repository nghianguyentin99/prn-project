<UserControl x:Class="PCShop.View.Admin.UserManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PCShop.View.Admin"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Background="White" Loaded="UserControl_Loaded">

    <UserControl.Resources>
        <local:RoleToStringConverter x:Key="RoleConverter"/>
        <local:ActiveToStringConverter x:Key="ActiveConverter"/>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" BorderBrush="#E0E0E0" BorderThickness="1" Padding="10" CornerRadius="5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Danh sách Tài khoản" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,10"/>

                <DataGrid Grid.Row="1" x:Name="dgUsers" AutoGenerateColumns="False" 
                          IsReadOnly="True" CanUserAddRows="False"
                          SelectionChanged="dgUsers_SelectionChanged">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding UserId}" Width="50"/>
                        <DataGridTextColumn Header="Tên đăng nhập" Binding="{Binding Username}" Width="150"/>
                        <DataGridTextColumn Header="Họ và Tên" Binding="{Binding FullName}" Width="*"/>
                        <DataGridTemplateColumn Header="Quyền" Width="100">
                        
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Role, Converter={StaticResource RoleConverter}}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="Trạng thái" Width="120"
    Binding="{Binding IsActive, Converter={StaticResource ActiveConverter}}"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <Border Grid.Column="1" Margin="20,0,0,0" BorderBrush="#E0E0E0" BorderThickness="1" Padding="15" CornerRadius="5" Background="#F9F9F9">
            <StackPanel>
                <TextBlock Text="Chi tiết Tài khoản" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,15"/>

                <Label Content="ID:" Padding="5,0"/>
                <TextBox x:Name="txtUserId" IsReadOnly="True" Background="#EEEEEE" Margin="0,0,0,10"/>

                <Label Content="Tên đăng nhập (*):" Padding="5,0"/>
                <TextBox x:Name="txtUsername" Margin="0,0,0,10"/>

                <Label Content="Họ và Tên:" Padding="5,0"/>
                <TextBox x:Name="txtFullName" Margin="0,0,0,10"/>

                <Label Content="Quyền (*):" Padding="5,0"/>
                <ComboBox x:Name="cmbRole" Margin="0,0,0,10">
                    <ComboBoxItem Content="Admin" Tag="0"/>
                    <ComboBoxItem Content="Staff" Tag="1"/>
                </ComboBox>

                <Label Content="Mật khẩu:" Padding="5,0"/>
                <PasswordBox x:Name="txtPassword" Margin="0,0,0,10"/>
                <TextBlock Text="Để trống nếu không muốn đổi mật khẩu." FontStyle="Italic" FontSize="11" Margin="0,-8,0,10"/>

                <Grid Margin="0,15,0,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button x:Name="btnNew" Content="Làm mới" Click="btnNew_Click" Margin="0,0,5,0" Background="#DDDDDD"/>
                    <Button x:Name="btnAdd" Content="Thêm mới" Grid.Column="1" Click="btnAdd_Click" Margin="5,0,0,0" Background="#27AE60" Foreground="White"/>
                </Grid>

                <Grid Margin="0,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button x:Name="btnUpdate" Content="Cập nhật" Click="btnUpdate_Click" Margin="0,0,5,0" Background="#2980B9" Foreground="White"/>
                    <Button x:Name="btnDelete" Content="Xóa (Vô hiệu hóa)" Grid.Column="1" Click="btnDelete_Click" Margin="5,0,0,0" Background="#C0392B" Foreground="White"/>
                </Grid>

            </StackPanel>
        </Border>
    </Grid>

</UserControl>