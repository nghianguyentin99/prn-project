<UserControl x:Class="PCShop.View.Admin.ReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="650" d:DesignWidth="1000"
             Background="White">
    <Grid Margin="20">
        <TextBlock Text="HỆ THỐNG BÁO CÁO CHI TIẾT" FontSize="24" FontWeight="Bold" Margin="0,0,0,20" Foreground="#2C3E50"/>

        <TabControl Margin="0,40,0,0" BorderThickness="0">

            <TabItem Header="LỊCH SỬ KHO VẬN" FontSize="14" Padding="15,10" Background="#ECF0F1">
                <Grid Margin="0,15,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Background="#F7F9F9" Padding="15" CornerRadius="5" Margin="0,0,0,15">
                        <StackPanel Orientation="Horizontal">
                            <Label Content="Từ ngày:" VerticalAlignment="Center" FontWeight="SemiBold"/>
                            <DatePicker x:Name="dpStockStart" Width="120" Margin="5,0"/>

                            <Label Content="Đến ngày:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="15,0,0,0"/>
                            <DatePicker x:Name="dpStockEnd" Width="120" Margin="5,0"/>

                            <Label Content="Loại giao dịch:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="15,0,0,0"/>
                            <ComboBox x:Name="cmbMoveType" Width="140" Margin="5,0" SelectedIndex="0" VerticalContentAlignment="Center">
                                <ComboBoxItem Content="-- Tất cả --" Tag="All"/>
                                <ComboBoxItem Content="Nhập kho (Import)" Tag="Import"/>
                                <ComboBoxItem Content="Xuất kho (Export)" Tag="Export"/>
                                <ComboBoxItem Content="Kiểm kê (Adjust)" Tag="Adjust"/>
                            </ComboBox>

                            <Button Content="Xem Dữ Liệu" Click="btnViewStock_Click" 
                                    Background="#3498DB" Foreground="White" Padding="20,6" Margin="20,0,0,0" FontWeight="Bold" BorderThickness="0"/>
                        </StackPanel>
                    </Border>

                    <DataGrid Grid.Row="1" x:Name="dgStockHistory" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" 
                              BorderBrush="#BDC3C7" BorderThickness="1" RowBackground="White" AlternatingRowBackground="#FDFEFE">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Thời gian" Binding="{Binding Date, StringFormat='dd/MM/yyyy HH:mm'}" Width="140"/>
                            <DataGridTextColumn Header="Sản phẩm" Binding="{Binding Product.Name}" Width="2*"/>
                            <DataGridTextColumn Header="Hành động" Binding="{Binding Type}" Width="100" FontWeight="SemiBold"/>
                            <DataGridTextColumn Header="Số lượng" Binding="{Binding Quantity}" Width="100" FontWeight="Bold"/>
                            <DataGridTextColumn Header="Tại Kho" Binding="{Binding Warehouse.WarehouseName}" Width="150"/>
                            <DataGridTextColumn Header="Người thực hiện" Binding="{Binding User.FullName}" Width="150"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <TabItem Header="TÀI CHÍNH (THU - CHI)" FontSize="14" Padding="15,10" Background="#ECF0F1">
                <Grid Margin="0,15,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Background="#F7F9F9" Padding="15" CornerRadius="5" Margin="0,0,0,15">
                        <StackPanel Orientation="Horizontal">
                            <Label Content="Kỳ báo cáo:" VerticalAlignment="Center" FontWeight="SemiBold"/>
                            <DatePicker x:Name="dpFinanceStart" Width="120" Margin="5,0"/>
                            <TextBlock Text="-" VerticalAlignment="Center" Margin="5,0"/>
                            <DatePicker x:Name="dpFinanceEnd" Width="120" Margin="5,0"/>

                            <Button Content="Phân Tích Tài Chính" Click="btnViewFinance_Click" 
                                    Background="#27AE60" Foreground="White" Padding="20,6" Margin="20,0,0,0" FontWeight="Bold" BorderThickness="0"/>
                        </StackPanel>
                    </Border>

                    <Grid Grid.Row="1" Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" Background="#E8F8F5" BorderBrush="#A2D9CE" BorderThickness="1" CornerRadius="5" Padding="15" Margin="0,0,10,0">
                            <StackPanel>
                                <TextBlock Text="TỔNG THU (BÁN HÀNG)" Foreground="#16A085" FontWeight="Bold"/>
                                <TextBlock x:Name="txtTotalRevenue" Text="0 ₫" FontSize="22" FontWeight="Bold" Foreground="#16A085" Margin="0,5,0,0"/>
                            </StackPanel>
                        </Border>

                        <Border Grid.Column="1" Background="#FDEBD0" BorderBrush="#F5CBA7" BorderThickness="1" CornerRadius="5" Padding="15" Margin="5,0,5,0">
                            <StackPanel>
                                <TextBlock Text="TỔNG CHI (NHẬP HÀNG)" Foreground="#D35400" FontWeight="Bold"/>
                                <TextBlock x:Name="txtTotalExpenditure" Text="0 ₫" FontSize="22" FontWeight="Bold" Foreground="#D35400" Margin="0,5,0,0"/>
                            </StackPanel>
                        </Border>

                        <Border Grid.Column="2" Background="#EBDEF0" BorderBrush="#D7BDE2" BorderThickness="1" CornerRadius="5" Padding="15" Margin="10,0,0,0">
                            <StackPanel>
                                <TextBlock Text="DÒNG TIỀN (THU - CHI)" Foreground="#8E44AD" FontWeight="Bold"/>
                                <TextBlock x:Name="txtNetCashFlow" Text="0 ₫" FontSize="22" FontWeight="Bold" Foreground="#8E44AD" Margin="0,5,0,0"/>
                            </StackPanel>
                        </Border>
                    </Grid>

                    <TabControl Grid.Row="2">
                        <TabItem Header="Chi tiết Bán hàng">
                            <DataGrid x:Name="dgSalesDetails" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" Margin="0,5,0,0">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Ngày bán" Binding="{Binding SaleDate, StringFormat='dd/MM/yyyy'}" Width="100"/>
                                    <DataGridTextColumn Header="Khách hàng" Binding="{Binding CustomerName}" Width="*"/>
                                    <DataGridTextColumn Header="Nhân viên" Binding="{Binding User.FullName}" Width="150"/>
                                    <DataGridTextColumn Header="Thành tiền" Binding="{Binding TotalAmount, StringFormat='N0'}" Width="120" FontWeight="Bold"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </TabItem>
                        <TabItem Header="Chi tiết Nhập hàng">
                            <DataGrid x:Name="dgImportDetails" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" Margin="0,5,0,0">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Ngày nhập" Binding="{Binding EntryDate, StringFormat='dd/MM/yyyy'}" Width="100"/>
                                    <DataGridTextColumn Header="Nhà cung cấp" Binding="{Binding Supplier.Name}" Width="*"/>
                                    <DataGridTextColumn Header="Nhân viên" Binding="{Binding User.FullName}" Width="150"/>
                                    <DataGridTextColumn Header="Tổng chi" Binding="{Binding TotalAmount, StringFormat='N0'}" Width="120" FontWeight="Bold"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </TabItem>
                    </TabControl>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>