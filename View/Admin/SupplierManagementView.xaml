<UserControl x:Class="PCShop.View.Admin.SupplierManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PCShop.View.Admin"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="800"
             Background="White" Loaded="UserControl_Loaded">

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="320"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" BorderBrush="#E0E0E0" BorderThickness="1" Padding="10" CornerRadius="5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Danh sách Nhà Cung Cấp" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,10"/>

                <DataGrid Grid.Row="1" x:Name="dgSuppliers" AutoGenerateColumns="False" 
                          IsReadOnly="True" CanUserAddRows="False"
                          SelectionChanged="dgSuppliers_SelectionChanged">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding SupplierId}" Width="50"/>
                        <DataGridTextColumn Header="Tên Nhà Cung Cấp" Binding="{Binding Name}" Width="150"/>
                        <DataGridTextColumn Header="Điện thoại" Binding="{Binding Phone}" Width="100"/>
                        <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="150"/>
                        <DataGridTextColumn Header="Địa chỉ" Binding="{Binding Address}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <Border Grid.Column="1" Margin="20,0,0,0" BorderBrush="#E0E0E0" BorderThickness="1" Padding="15" CornerRadius="5" Background="#F9F9F9">
            <StackPanel>
                <TextBlock Text="Chi tiết Nhà Cung Cấp" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,15"/>

                <Label Content="ID:" Padding="5,0"/>
                <TextBox x:Name="txtSupplierId" IsReadOnly="True" Background="#EEEEEE" Margin="0,0,0,10"/>

                <Label Content="Tên Nhà Cung Cấp (*):" Padding="5,0"/>
                <TextBox x:Name="txtName" Margin="0,0,0,10"/>

                <Label Content="Điện thoại:" Padding="5,0"/>
                <TextBox x:Name="txtPhone" Margin="0,0,0,10"/>

                <Label Content="Email:" Padding="5,0"/>
                <TextBox x:Name="txtEmail" Margin="0,0,0,10"/>

                <Label Content="Địa chỉ:" Padding="5,0"/>
                <TextBox x:Name="txtAddress" Height="60" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,0,0,10"/>

                <Grid Margin="0,15,0,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button x:Name="btnNew" Content="Làm mới" Click="btnNew_Click" Margin="0,0,5,0" Background="#DDDDDD"/>
                    <Button x:Name="btnAdd" Content="Thêm mới" Grid.Column="1" Click="btnAdd_Click" Margin="5,0,0,0" Background="#27AE60" Foreground="White"/>
                </Grid>

                <Grid Margin="0,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button x:Name="btnUpdate" Content="Cập nhật" Click="btnUpdate_Click" Margin="0,0,5,0" Background="#2980B9" Foreground="White"/>
                    <Button x:Name="btnDelete" Content="Xóa" Grid.Column="1" Click="btnDelete_Click" Margin="5,0,0,0" Background="#C0392B" Foreground="White"/>
                </Grid>

            </StackPanel>
        </Border>
    </Grid>
</UserControl>